<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.2/css/bootstrap.min.css"
          integrity="sha384-Smlep5jCw/wG7hdkwQ/Z5nLIefveQRIY9nfy6xoR1uRYBtpZgI6339F5dgvm/e9B" crossorigin="anonymous">
    <link rel="stylesheet" href="https://bootswatch.com/4/minty/bootstrap.min.css">
    {% block stylesheets %}{% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">StreetDeals</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/main">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('security_login') }}">Mon compte</a>
            </li>


        </ul>
        <div class="annonce" style="color: red;">
            <li class="nav-item btn btn-dark">
                <a class="nav-link" href="{{ path('annonce') }}">Publier une annonce</a>
            </li>
        </div>

        {#Connexiot et deconnxion au site #}

        {% if not app.user %}
            <div class="login">
                <li class="nav-item btn btn-light">
                    <a class="nav-link" href="{{ path('security_login') }}">Connexion</a>
                </li>
            </div>

            {##}fonction pour afficher le bouton connexion et déconnexion
            selon si l'utilisateur est connecté#}

        {% else %}
            <div class="logout" style="color: #e61910;">
                <li class="nav-item btn btn-danger">
                    <a class="nav-link" href="{{ path('security_login') }}">Déconnexion</a>
                </li>
            </div>
        {% endif %}
        {#<form class="form-inline my-2 my-lg-0">#}
        {#<input class="form-control mr-sm-2" placeholder="Search" type="text">#}
        {#<button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>#}
        {#</form>#}
    </div>
</nav>
<div class="container">
    {% block body %}{% endblock %}
</div>
{% block javascripts %}{% endblock %}
</body>
</html>
